# SQL Folder

This folder contains SQL scripts for database setup, queries, and migrations.

## Usage

Run SQL files using your database client.

Example:

```bash
mysql -u username -p database_name < script.sql